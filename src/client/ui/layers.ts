/**
 * 重なり順を 1 か所に集める。
 *
 * これまで z-10 / z-20 / z-40 / z-50 / z-[60] / z-[100] が各ファイルに散っていて、
 * 「とりあえず上に出したい」だけの任意値が混ざっていた。段を名前で持つと、
 * 新しい浮遊要素を足すときにどこへ挟むかを考えざるを得なくなる。
 *
 * speechBar を drawer より下げてあるのは意図的で、モバイルでサイドバーを
 * 開いたときに再生バーが暗幕の上に残らないようにするため。
 */
export const LAYER = {
  /** ポップオーバーの外側クリック用の透明な膜 */
  popoverBackdrop: 'z-10',
  /** ポップオーバー本体・画面上部のヘッダー */
  popoverPanel: 'z-20',
  header: 'z-20',
  /** 下部の読み上げバー（ドロワーの下に潜る） */
  speechBar: 'z-30',
  drawer: 'z-40',
  modal: 'z-50',
  /** モーダルの上に出すモーダル（確認ダイアログ） */
  modalTop: 'z-60',
  toast: 'z-[100]',
} as const;
